/**
 * Default brand primary color for the shadcn registry distribution.
 *
 * SSOT: `packages/backend/convex/shared/constants.ts` `DEFAULT_BRAND_PRIMARY`.
 * This file exists ONLY so installable @reflet/ui consumers (who don't depend
 * on the backend) have access. App code (apps/web + backend) MUST import from
 * `@reflet/backend/convex/shared/constants`.
 *
 * Keep this value in sync with the backend constant.
 */
export const DEFAULT_BRAND_PRIMARY = "#5c6d4f";
