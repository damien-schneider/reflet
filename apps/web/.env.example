# ----------------------------- Convex ---------------------------
# Get from: https://dashboard.convex.dev
NEXT_PUBLIC_CONVEX_URL="https://your-deployment.convex.cloud"
NEXT_PUBLIC_CONVEX_SITE_URL="https://your-deployment.convex.site"

# ----------------------------- Site URL ---------------------------
SITE_URL="http://localhost:3003"

# ----------------------------- Auth ---------------------------
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=
BETTER_AUTH_URL="http://localhost:3003"
CORS_ORIGIN="http://localhost:3003"

# ----------------------------- GitHub App -----------------------------
# Create a GitHub App: https://github.com/settings/apps
# Required permissions: Repository contents (read), Issues (read/write)
# Webhook URL: https://your-deployment.convex.site/github-webhook
GITHUB_APP_ID=
GITHUB_APP_SLUG=
GITHUB_WEBHOOK_SECRET=
GITHUB_APP_PRIVATE_KEY=

# ----------------------------- VAPID (Push Notifications) ----------
# Generate with: bunx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=

# ----------------------------- E2E Testing -----------------------------
# Skip email verification for e2e tests (set to "true" to enable)
# NEXT_PUBLIC_SKIP_EMAIL_VERIFICATION="true"

# ----------------------------- Stripe (Reference) -----------------------------
# Note: Stripe keys are stored in Convex environment variables, not here.
# The frontend only needs the Convex URL to communicate with the backend.
#
# To set up Stripe for your deployment:
# 1. Create products at: https://dashboard.stripe.com/products
#    - Pro Monthly: $29/month recurring
#    - Pro Yearly: $290/year recurring
#
# 2. Create webhook at: https://dashboard.stripe.com/webhooks
#    - Endpoint URL: https://your-deployment.convex.site/stripe/webhook
#    - Events: checkout.session.completed, customer.created,
#              customer.subscription.created/updated/deleted,
#              invoice.paid, invoice.payment_failed, payment_intent.succeeded
#
# 3. Set Convex environment variables:
#    npx convex env set STRIPE_SECRET_KEY="sk_test_..."
#    npx convex env set STRIPE_WEBHOOK_SECRET="whsec_..."
#    npx convex env set STRIPE_PRICE_PRO_MONTHLY="price_..."
#    npx convex env set STRIPE_PRICE_PRO_YEARLY="price_..."
